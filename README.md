# docker-mingw-w64-32

```
git clone https://github.com/kjunichi/mruby-webcam-docker.git
cd mruby-webcam-docker
docker build -t kjunichi/mruby-webcam .
docker run -it kjunichi/mruby-webcam ls src/mruby/build
```
